distribute result [$$27]
-- DISTRIBUTE_RESULT  |PARTITIONED|
  exchange
  -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
    project ([$$27])
    -- STREAM_PROJECT  |PARTITIONED|
      assign [$$27] <- [{"staff": $$28, "student": $$29}]
      -- ASSIGN  |PARTITIONED|
        project ([$$28, $$29])
        -- STREAM_PROJECT  |PARTITIONED|
          exchange
          -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
            join (interval-overlaps($$30, $$31))
            -- INTERVAL_FORWARD_SCAN_JOIN [$$30] [$$31]  |PARTITIONED|
              exchange
              -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                order (ASC, $$30)
                -- STABLE_SORT [$$30(ASC)]  |PARTITIONED|
                  exchange
                  -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                    project ([$$28, $$30])
                    -- STREAM_PROJECT  |PARTITIONED|
                      exchange
                      -- PARTIAL_BROADCAST_RANGE_INTERSECT_EXCHANGE [{$$34,$$35,ASC}] RANGE_MAP:{SPLIT:3}  |PARTITIONED|
                        assign [$$34, $$35] <- [get-interval-start($$30), get-interval-end($$30)]
                        -- ASSIGN  |PARTITIONED|
                          project ([$$28, $$30])
                          -- STREAM_PROJECT  |PARTITIONED|
                            assign [$$30] <- [$$f.getField(2)]
                            -- ASSIGN  |PARTITIONED|
                              exchange
                              -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                                data-scan []<-[$$28, $$f] <- TinyCollege.Staff
                                -- DATASOURCE_SCAN  |PARTITIONED|
                                  exchange
                                  -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                                    empty-tuple-source
                                    -- EMPTY_TUPLE_SOURCE  |PARTITIONED|
              exchange
              -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                order (ASC, $$31)
                -- STABLE_SORT [$$31(ASC)]  |PARTITIONED|
                  exchange
                  -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                    project ([$$29, $$31])
                    -- STREAM_PROJECT  |PARTITIONED|
                      exchange
                      -- RANGE_PARTITION_EXCHANGE [$$36(ASC)] RANGE_MAP:{SPLIT:3}  |PARTITIONED|
                        project ([$$29, $$31, $$36])
                        -- STREAM_PROJECT  |PARTITIONED|
                          assign [$$36, $$37] <- [get-interval-start($$31), get-interval-end($$31)]
                          -- ASSIGN  |PARTITIONED|
                            project ([$$29, $$31])
                            -- STREAM_PROJECT  |PARTITIONED|
                              assign [$$31] <- [$$d.getField(2)]
                              -- ASSIGN  |PARTITIONED|
                                exchange
                                -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                                  data-scan []<-[$$29, $$d] <- TinyCollege.Students
                                  -- DATASOURCE_SCAN  |PARTITIONED|
                                    exchange
                                    -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                                      empty-tuple-source
                                      -- EMPTY_TUPLE_SOURCE  |PARTITIONED|