distribute result [$$27]
-- DISTRIBUTE_RESULT  |PARTITIONED|
  exchange
  -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
    project ([$$27])
    -- STREAM_PROJECT  |PARTITIONED|
      assign [$$27] <- [{"staff": $$30, "student": $$31}]
      -- ASSIGN  |PARTITIONED|
        project ([$$30, $$31])
        -- STREAM_PROJECT  |PARTITIONED|
          select (le($$34, $$35))
          -- STREAM_SELECT  |PARTITIONED|
            project ([$$30, $$34, $$31, $$35])
            -- STREAM_PROJECT  |PARTITIONED|
              exchange
              -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                join (eq($$32, $$33))
                -- HYBRID_HASH_JOIN [$$32][$$33]  |PARTITIONED|
                  exchange
                  -- HASH_PARTITION_EXCHANGE [$$32]  |PARTITIONED|
                    project ([$$30, $$32, $$34])
                    -- STREAM_PROJECT  |PARTITIONED|
                      assign [$$34, $$32] <- [get-interval-end($$28), get-interval-start($$28)]
                      -- ASSIGN  |PARTITIONED|
                        project ([$$30, $$28])
                        -- STREAM_PROJECT  |PARTITIONED|
                          assign [$$28] <- [$$f.getField(2)]
                          -- ASSIGN  |PARTITIONED|
                            exchange
                            -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                              data-scan []<-[$$30, $$f] <- TinyCollege.Staff
                              -- DATASOURCE_SCAN  |PARTITIONED|
                                exchange
                                -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                                  empty-tuple-source
                                  -- EMPTY_TUPLE_SOURCE  |PARTITIONED|
                  exchange
                  -- HASH_PARTITION_EXCHANGE [$$33]  |PARTITIONED|
                    project ([$$31, $$33, $$35])
                    -- STREAM_PROJECT  |PARTITIONED|
                      assign [$$35, $$33] <- [get-interval-end($$29), get-interval-start($$29)]
                      -- ASSIGN  |PARTITIONED|
                        project ([$$31, $$29])
                        -- STREAM_PROJECT  |PARTITIONED|
                          assign [$$29] <- [$$d.getField(2)]
                          -- ASSIGN  |PARTITIONED|
                            exchange
                            -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                              data-scan []<-[$$31, $$d] <- TinyCollege.Students
                              -- DATASOURCE_SCAN  |PARTITIONED|
                                exchange
                                -- ONE_TO_ONE_EXCHANGE  |PARTITIONED|
                                  empty-tuple-source
                                  -- EMPTY_TUPLE_SOURCE  |PARTITIONED|